{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "cd tracker_development && PYTHONPATH=.. python manage.py migrate && PYTHONPATH=.. daphne -b 0.0.0.0 -p $PORT tracker_development.asgi:application"
  }
}
